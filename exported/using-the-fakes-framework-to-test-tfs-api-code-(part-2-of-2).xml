<?xml version="1.0" encoding="utf-8"?>
<post>
  <id>3cd2ea61-be47-4966-a7b2-ad8c39884231</id>
  <title>Using the Fakes Framework to Test TFS API Code (Part 2 of 2)</title>
  <slug>using-the-fakes-framework-to-test-tfs-api-code-(part-2-of-2)</slug>
  <shortUrl>http://bit.ly/PS1pqX</shortUrl>
  <author>Colin Dembovsky</author>
  <pubDate>2012-03-20 13:22:00</pubDate>
  <lastModified>2020-04-02 11:18:31</lastModified>
  <content>&lt;p&gt;In &lt;a href="http://colinsalmcorner.blogspot.com/2012/03/using-fakes-framework-to-test-tfs-api.html" target="_blank"&gt;Part 1&lt;/a&gt;, we started faking some TFS objects. We got as far as faking the TeamProjectCollection and WorkItemStore. In this post, we’ll complete the test for copying work items by providing a fake QueryHierarchy and a fake list of WorkItems.&lt;/p&gt; &lt;h2&gt;Binding IEnumerables&lt;/h2&gt; &lt;p&gt;Since the QueryHierarchy is an IEnumerable, we’ll need to either fake the GetEnumerator() method, or find a way of making the fake QueryHierarchy enumerable! The problem with trying to fake the GetEnumerator() method is that you end up in an infinite loop – if you’re trying to get the enumerator for sub nodes of the hierarchy, you’ll also need to call GetEnumerator() which will call GetEnumerator() and so on and so on… so we’ll see if we can make the fake QueryHiearchy behave like an enumerable object. Which is really quite easy when you know how!&lt;/p&gt; &lt;p&gt;We create a list of QueryFolders (outside the ShimsContext, since we want these object to be real and not fake) and then call the Bind() method on the fake QueryHierarchy:&lt;/p&gt;&lt;pre class="brush: csharp; ruler: true;"&gt;var myQueries = new QueryFolder("My Queries");&lt;br /&gt;var sharedQueries = new QueryFolder("Team Queries");&lt;br /&gt;sharedQueries.Add(new QueryDefinition("My Tasks", "SELECT System.Id FROM WorkItems WHERE System.WorkItemType = 'Task'"));&lt;br /&gt;sharedQueries.Add(new QueryDefinition("My Bugs", "SELECT System.Id FROM WorkItems WHERE System.WorkItemType = 'Bug'"));&lt;br /&gt;var topQueries = new List&lt;QueryItem&gt;() { myQueries, sharedQueries };&lt;br /&gt;&lt;br /&gt;using (ShimsContext.Create())&lt;br /&gt;{&lt;br /&gt;    // set up the fakes&lt;br /&gt;    var fakeHierarchy = new ShimQueryHierarchy();&lt;br /&gt;    fakeHierarchy.Bind(topQueries);&lt;br /&gt;&lt;/pre&gt;&lt;br /&gt;&lt;p&gt;Now when we run the code, the QueryHierarchy behaves like an enumeration and a call to find the “My Tasks” query will result in a QueryDefinition being returned!&lt;/p&gt;&lt;br /&gt;&lt;h2&gt;Faking an Interface&lt;/h2&gt;&lt;br /&gt;&lt;p&gt;The next thing we’ll need to fake is the RunQuery() method on the Query. The first snag we’ll hit is that the RunQuery() method in the WorkItemCopyer uses the IGroupSecurityService to resolve the current user’s display name for any @me macros in the query. So we’ll first need to fake that. Hang on – how do you fake an interface?? Enter Stubs.&lt;/p&gt;&lt;br /&gt;&lt;p&gt;So far we’ve only used Shims. For faking interfaces, we’ll need to switch to Stubs. The only method on the IGroupSecurityService interface that we want to fake is the ReadIdentity() method. So we can fake that and return a fake Identity object. Again, we’ll run into the problem of wanting a real object in a fake world (i.e. the ShimsContext) so we’ll use a sneaky way of working around that. Here’s the code for the fake IGroupSecurityService:&lt;/p&gt;&lt;pre class="brush: csharp; ruler: true;"&gt;var fakeSecurityService = new StubIGroupSecurityService()&lt;br /&gt;{&lt;br /&gt;    ReadIdentitySearchFactorStringQueryMembership = (searchFactor, criteria, identity) =&gt; ShimsContext.ExecuteWithoutShims&lt;Identity&gt;(() =&gt; new Identity() { DisplayName = "Bob" })&lt;br /&gt;};&lt;br /&gt;ShimTfsConnection.AllInstances.GetServiceOf1&lt;IGroupSecurityService&gt;((t) =&gt; fakeSecurityService);&lt;br /&gt;&lt;/pre&gt;&lt;br /&gt;&lt;p&gt;This will create the Identity object “outside” the current ShimsContext. Of course, we need to tell the TfsConnection GetService&lt;T&gt; call to return the fake security service when asked for an IGroupSecurityService (which is done in the last line above).&lt;/p&gt;&lt;br /&gt;&lt;h2&gt;Faking Constructors&lt;/h2&gt;&lt;br /&gt;&lt;p&gt;The next thing that the WorkItemCopyer code does is instantiate a Query object in order to execute the Work Item Query. We’ll need to fake the constructor in order to return a fake Query. The method we particularly want to fake out on the Query object is the RunQuery() method, which is going to return a WorkItemCollection. We’ll want to create a list of Work Items and bind a fake WorkItemCollection to the list so that we can enumerate the fake results. There is also a call to the IterationPath setter and a couple of getters on the WorkItem itself, so we’ll fake that at the same time. Here’s the code:&lt;/p&gt;&lt;pre class="brush: csharp; ruler: true;"&gt;ShimQuery.ConstructorWorkItemStoreStringIDictionary = (q, store, text, dict) =&gt;&lt;br /&gt;{&lt;br /&gt;    new ShimQuery(q)&lt;br /&gt;    {&lt;br /&gt;        RunQuery = () =&gt;&lt;br /&gt;        {&lt;br /&gt;            var workItemType = new ShimWorkItemType()&lt;br /&gt;            {&lt;br /&gt;                NameGet = () =&gt; "Task"&lt;br /&gt;            };&lt;br /&gt;            var list = new List&lt;WorkItem&gt;()&lt;br /&gt;            {&lt;br /&gt;                new ShimWorkItem()&lt;br /&gt;                {&lt;br /&gt;                    IdGet = () =&gt; 12,&lt;br /&gt;                    TitleGet = () =&gt; "Some Work Item",&lt;br /&gt;                    TypeGet = () =&gt; workItemType,&lt;br /&gt;                    IterationPathSetString = (path) =&gt; { },&lt;br /&gt;                }&lt;br /&gt;            };&lt;br /&gt;            var workItems = new ShimWorkItemCollection()&lt;br /&gt;            {&lt;br /&gt;                CountGet = () =&gt; list.Count&lt;br /&gt;            };&lt;br /&gt;            workItems.Bind(list);&lt;br /&gt;            return workItems;&lt;br /&gt;        }&lt;br /&gt;    };&lt;br /&gt;};&lt;br /&gt;&lt;/pre&gt;&lt;br /&gt;&lt;p&gt;Finally, we’ll create a couple of counters to make sure that our code calls the copy and the save methods of the work items for each work item.&lt;/p&gt;&lt;pre class="brush: csharp; ruler: true;"&gt;var copyCount = 0;&lt;br /&gt;ShimWorkItem.AllInstances.Copy = (w) =&gt;&lt;br /&gt;{&lt;br /&gt;    copyCount++;&lt;br /&gt;    return new ShimWorkItem(w);&lt;br /&gt;};&lt;br /&gt;var saveCount = 0;&lt;br /&gt;ShimWorkItem.AllInstances.Save = (_) =&gt; saveCount++;&lt;br /&gt;&lt;br /&gt;// test instantiate&lt;br /&gt;var target = new WorkItemCopyer(fakeTPC, "Code11");&lt;br /&gt;Assert.IsNotNull(target);&lt;br /&gt;&lt;br /&gt;// test find query&lt;br /&gt;var query = target.FindQuery("My Tasks");&lt;br /&gt;Assert.IsNotNull(query);&lt;br /&gt;&lt;br /&gt;// test run query&lt;br /&gt;target.RunQuery(query);&lt;br /&gt;Assert.AreEqual(1, target.WorkItems.Count);&lt;br /&gt;&lt;br /&gt;// test copy work items&lt;br /&gt;var count = target.CopyWorkItems("Test Iteration");&lt;br /&gt;Assert.AreEqual(1, count);&lt;br /&gt;Assert.AreEqual(1, copyCount);&lt;br /&gt;Assert.AreEqual(1, saveCount);&lt;br /&gt;&lt;/pre&gt;&lt;br /&gt;&lt;p&gt;Voila! We’ve been able to test our code without actually connecting to a real TFS service. And coverage? Well, it’s up to 97% for the WorkItemCopyer class. Not too bad!&lt;/p&gt;&lt;br /&gt;&lt;p&gt;The completed solution is available on my &lt;a href="https://skydrive.live.com/redir.aspx?cid=64a24e0938d6d062&amp;resid=64A24E0938D6D062!335&amp;parid=64A24E0938D6D062!334" target="_blank"&gt;skydrive&lt;/a&gt;.&lt;/p&gt;&lt;br /&gt;&lt;p&gt;(More) Happy Faking!&lt;/p&gt;  </content>
  <ispublished>true</ispublished>
  <categories>
    <category>TFS API</category>
    <category>Testing</category>
  </categories>
  <comments></comments>
  <viewCount>2104</viewCount>
</post>